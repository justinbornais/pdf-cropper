# pdf-cropper
A tool to allow users to crop a PDF document from within the middle of the pages.

Run the frontend of the application via `npm run start` and the backend via `uvicorn app:app --reload`.